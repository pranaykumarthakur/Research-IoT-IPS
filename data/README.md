# Dataset

This directory contains the dataset used for the project.

The raw dataset (`RT_IoT2022.csv`) is kept in `data/raw/` and must remain unchanged. Any generated or processed data during execution will be structured here or in `data/processed/`.
